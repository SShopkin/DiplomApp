<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/accentOrange</item>
        <item name="colorPrimaryDark">@color/accentOrange</item>
        <item name="colorAccent">@color/accentOrange</item>
    </style>
    <style name="purple" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/accentPurple</item>
        <item name="colorPrimaryDark">@color/accentPurple</item>
        <item name="colorAccent">@color/accentPurple</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
